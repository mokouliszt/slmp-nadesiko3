# デバイスの書き込み例
!「nadesiko3-slmp」を取り込む。

設定は{"ホスト":"192.168.3.39","ポート":5007,"機種":"melsec:iq-r"}
接続は、設定でSLMP接続。

# ワード書き込み
接続で[100,200,300]を「D100」にSLMPワード書。

# ビット書き込み
接続で[オン,オフ,オン]を「M0」にSLMPビット書。

# 32ビット値・実数
接続で[123456]を「D300」にSLMPダブルワード書。
接続で[36.5]を「D400」にSLMP実数書。

「書き込み完了」と表示。
接続をSLMP切断。
